package com.example.SpringBootJPAMultiDataSource.entity2;

import javax.persistence.*;

@Entity
@Table(name = "advertisers", schema = "advertiser", catalog = "")
@NamedStoredProcedureQuery(name = "sp_GetAdvertiser",procedureName = "sp_GetAdvertiser",resultClasses = AdvertisersEntity.class)
public class AdvertisersEntity {
    private long id;
    private String name;

    @Id
    @Column(name = "ID")
    public long getId() {
        return id;
    }

    public void setId(long id) {
        this.id = id;
    }

    @Basic
    @Column(name = "NAME")
    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;

        AdvertisersEntity that = (AdvertisersEntity) o;

        if (id != that.id) return false;
        if (name != null ? !name.equals(that.name) : that.name != null) return false;

        return true;
    }

    @Override
    public int hashCode() {
        int result = (int) (id ^ (id >>> 32));
        result = 31 * result + (name != null ? name.hashCode() : 0);
        return result;
    }
}
